import axios from 'axios';
import cheerio from 'cheerio';

class Scraper {
  constructor(url, options) {
    this.url = url;
  }

  login(path, options) {

  }

  scrape(path, options) {

  }
};

export default Scraper;
